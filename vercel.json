{
  "buildCommand": "cd mmv-warehouse && npm install && npm run build",
  "outputDirectory": "mmv-warehouse/dist",
  "installCommand": "echo skip-root-install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
